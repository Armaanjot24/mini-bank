# --- database ---
PyMySQL>=1.1.0          # pure-Python MySQL driver: no compiler needed on Python 3.14
cryptography>=42.0      # required by PyMySQL for MySQL 9's caching_sha2_password auth
python-dotenv>=1.0.0    # loads MYSQL_* from .env

# --- security ---
bcrypt>=4.1.0           # password hashing (used directly, no passlib wrapper)

# --- ML / analysis (Phases 7-9) ---
pandas>=2.2.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
joblib>=1.3.0

# --- API (Phase 10) ---
fastapi>=0.110.0
uvicorn>=0.29.0

# --- testing (Phase 11) ---
pytest>=8.0.0
